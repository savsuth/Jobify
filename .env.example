ANTHROPIC_API_KEY=
DATABASE_URL=postgresql+psycopg://localhost:5432/job_application_agent
# Optional. Raises the GitHub REST API rate limit from 60/hr to 5000/hr - not
# required for normal use (the Resume Agent fetches a handful of repos once
# per pipeline run).
GITHUB_TOKEN=
